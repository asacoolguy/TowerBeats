using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CentralTower : MonoBehaviour {
    public Color mainColor;


	private void Start () {
		// set color of build panel

        // set up initial power states

        // todo: gameManager will call hover and selected function for central tower
        // central tower needs to react appropriately to hover and select
	}
	
	
	private void Update () {
	}
}
